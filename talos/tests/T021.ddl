-- Zoo4 from notes.org. Function call result entanglement

def P (a : int) (c: int) = {
    @b = UInt8 as int;
    c > 7 is true; 
    ^ a + b; 
}

def Main = {
    x = UInt8 as int;
    z = P x x; 
    z < 10 is true;
}
