#ifndef DDL_MAP
#define DDL_MAP

namespace DDL {

template <typename k, typename v>
class Map {
  // XXX
};


template <typename k, typename v>
class MapBuilder {
  // XXX
};

template <typename k, typename v>
class MapIterator {
  // XXX
};

}

#endif
