[package]
name = "daedalus-rts-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
serde       = { version = "1.0", features = ["derive"] }
serde_json  = "1.0"
num-bigint  = { version = "0.4.3", features = ["serde"] }
num-traits  = "0.2"

[features]
default = []
detailed-errors = []