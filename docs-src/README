This folder contains the sources for the Daedalus manual.

The source for the manual is in `source/index.rst`.

Prerequsits:

    pip install sphinx_rtd_theme
    pip install sphinx_design

To build a PDF or html document use:

    make latexpdf
    make html

The result will be in a subdirectory of `build`.

To update the documentation on the github pages, use:

    make github

This will build the `html` documentation and copy it to the `docs` folder,
which is what the github pages show.

