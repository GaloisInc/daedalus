// Test: MalformedType (wrong number of numeric arguments)
// This test provides wrong number of numeric arguments to 'uint'

import spec(Main)

using CPP

extern ->
  #include "main_parser.h"
  #include <cstdint>

extern def ->
  #include "exporter.h"

type Result -> uint32_t

// Error: uint expects 1 numeric argument, but given 0
def exportMain(x: uint): Result -> 42
