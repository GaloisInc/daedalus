# Rust PDF workspace

This workspace contains:

- `pdfcos`, the PDF COS parsing and reference-resolution library.
- `text-extract`, an application for PDF text extraction.

Generate the PDF COS parser and build both crates with:

```text
make
```
